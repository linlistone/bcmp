/**
 * 全局后台服务映射表
 * created by helin3 2017-12-04
 */
(function (window) {
  window.backend = {
    appOcaService: '/api',
    adminService: '/api',
    uaaService: '/api',
    bcmpService: '/api',
    generatorService: '/api', // 代码生成
    sysId: 'bcmp'// 默认系统ID
  };
}(window));
